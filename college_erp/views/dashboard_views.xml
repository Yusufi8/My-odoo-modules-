<odoo>
    <record id="view_college_dashboard_kanban" model="ir.ui.view">
        <field name="name">college.dashboard.kanban</field>
        <field name="model">college.dashboard</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_small_column">
                <templates>
                    <t t-name="kanban-box">
                        <div class="o_kanban_record">
                            <div class="oe_kanban_global_click">
                                <h3>📊 College Dashboard</h3>
                                <ul>
                                    <li><b>Total Students:</b> <t t-esc="record.student_count.value"/></li>
                                    <li><b>Total Departments:</b> <t t-esc="record.department_count.value"/></li>
                                    <li><b>Total Fees Collected:</b> ₹ <t t-esc="record.total_fees.value"/></li>
                                </ul>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="action_college_dashboard" model="ir.actions.act_window">
        <field name="name">Dashboard</field>
        <field name="res_model">college.dashboard</field>
        <field name="view_mode">kanban</field>
    </record>

    <menuitem id="menu_college_dashboard" name="Dashboard" parent="menu_college_root" action="action_college_dashboard"/>
</odoo>
